let frutas = ["Manzana", "Banana", "Naranja"]

// Recordemos el metodo length, que nos devuelve el largo del array
// lenGTH
// Es el error más común con el array equivocarse cómo se escribe length
// Memotécnico del GTH del final que es lo que mas se confunde:
// Gallina tiene Huevos / Go to Hell
console.log(frutas.length)

for (let i = 0; i < frutas.length; i++) {      
     console.log(frutas[i]) // recorrerá todos los elementos del array
}


/* for (let i = 0; i < frutas.length; i++) {
    
}
*/   // Es como decir: iterá/repetí esto tantas veces como elementos tenga el array 

